// sdk/javascript/merchi.js

// Other function and object definitions...

function getQueryStringValue (key) {
  // Check if the window object is defined
  if (typeof window === 'undefined') {
    // If not, return undefined immediately
    return undefined;
  }

  // Rest of the function implementation...
}

// Other function and object exports...
